<!DOCTYPE html>

<html class="dark" lang="en">

<head>
  <meta charset="utf-8" />
  <meta content="width=device-width, initial-scale=1.0" name="viewport" />
  <title>Add New Product | Football Pro Admin</title>
  <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
  <link
    href="https://fonts.googleapis.com/css2?family=Lexend:wght@400;500;600;700;800;900&family=Noto+Sans:wght@400;500;600;700&display=swap"
    rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap"
    rel="stylesheet" />
  <!-- SweetAlert2 -->
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <!-- Cropper.js -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.6.2/cropper.min.css" />
  <script src="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.6.2/cropper.min.js"></script>

  <script id="tailwind-config">
    tailwind.config = {
      darkMode: "class",
      theme: {
        extend: {
          colors: {
            primary: "#137fec",
            "background-light": "#f6f7f8",
            "background-dark": "#101922",
            "surface-dark": "#1c2632",
          },
          fontFamily: {
            display: ["Lexend", "sans-serif"],
            body: ["Noto Sans", "sans-serif"],
          },
          borderRadius: {
            DEFAULT: "0.25rem",
            lg: "0.5rem",
            xl: "0.75rem",
            full: "9999px",
          },
        },
      },
    };
  </script>
  <style>
    .material-symbols-outlined {
      font-variation-settings:
        "FILL" 0,
        "wght" 400,
        "GRAD" 0,
        "opsz" 24;
    }

    body {
      font-family: "Noto Sans", sans-serif;
    }

    h1,
    h2,
    h3,
    h4,
    .font-display {
      font-family: "Lexend", sans-serif;
    }

    @keyframes spin {
      from {
        transform: rotate(0deg);
      }

      to {
        transform: rotate(360deg);
      }
    }

    .animate-spin {
      animation: spin 1s linear infinite;
      display: inline-block;
    }
  </style>
</head>

<body class="bg-background-light dark:bg-background-dark text-slate-900 dark:text-slate-100 min-h-screen">
  <div class="flex">
    <!-- Sidebar Navigation -->
    <%- include('partials/sidebar', { activePage: 'products' }) %>

    <!-- Main Content -->
    <main class="ml-64 flex-1 min-h-screen bg-background-dark flex flex-col">
      <!-- Top Bar -->
      <header
        class="sticky top-0 z-40 bg-[#101922]/95 backdrop-blur-md border-b border-white/5 flex items-center justify-between px-8 h-16 w-full">
        <h2 class="text-xl font-bold text-white tracking-tight font-display">
          Add New Product
        </h2>
        <div class="flex items-center gap-4">
          <button class="material-symbols-outlined text-slate-400 hover:text-white">
            notifications
          </button>
          <button class="material-symbols-outlined text-slate-400 hover:text-white">
            settings
          </button>
        </div>
      </header>

      <div class="flex-1 overflow-y-auto">
        <div class="max-w-5xl mx-auto p-6 md:p-10 space-y-8 pb-10">

          <!-- ═══════════════ FORM ═══════════════ -->
          <form id="addProductForm" enctype="multipart/form-data" novalidate>
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">

              <!-- Left Column (Main Form Content) -->
              <div class="lg:col-span-2 space-y-8">

                <!-- General Information Section -->
                <section class="bg-[#111418] border border-[#1c2632] rounded-xl overflow-hidden">
                  <div class="p-6 border-b border-[#1c2632]">
                    <h3 class="text-lg font-bold text-white flex items-center gap-2 font-display">
                      <span class="material-symbols-outlined text-primary">info</span>
                      General Information
                    </h3>
                  </div>
                  <div class="p-6 space-y-6">

                    <!-- Product Name -->
                    <div class="space-y-1">
                      <label for="productName" class="text-sm font-bold text-slate-300">Product Name <span
                          class="text-red-400">*</span></label>
                      <input id="productName" name="name"
                        class="w-full bg-[#101922] border border-[#1c2632] rounded-lg focus:ring-primary focus:border-primary text-white px-3 py-2"
                        placeholder="e.g. Pro Grip Goalkeeper Gloves" type="text" />
                      <p id="nameError" class="error-msg hidden text-red-400 text-xs mt-1"></p>
                    </div>

                    <!-- Category -->
                    <div class="space-y-1">
                      <label for="productCategory" class="text-sm font-bold text-slate-300">Category <span
                          class="text-red-400">*</span></label>
                      <select id="productCategory" name="category"
                        class="w-full bg-[#101922] border border-[#1c2632] rounded-lg focus:ring-primary focus:border-primary text-white px-3 py-2">
                        <option value="">Select Category</option>
                        <% if (typeof categories !=='undefined' ) { %>
                          <% categories.forEach(cat=> { %>
                            <option value="<%= cat._id %>">
                              <%= cat.name %>
                            </option>
                            <% }); %>
                              <% } %>
                      </select>
                      <p id="categoryError" class="error-msg hidden text-red-400 text-xs mt-1"></p>
                    </div>

                    <!-- Description -->
                    <div class="space-y-1">
                      <label for="productDescription" class="text-sm font-bold text-slate-300">Description <span
                          class="text-red-400">*</span></label>
                      <div class="border border-[#1c2632] rounded-lg overflow-hidden">
                        <textarea id="productDescription" name="description"
                          class="w-full bg-[#101922] border-none focus:ring-0 text-white p-3 font-body"
                          placeholder="Describe the material, fit, and features..." rows="6"></textarea>
                      </div>
                      <p id="descriptionError" class="error-msg hidden text-red-400 text-xs mt-1"></p>
                    </div>

                  </div>
                </section>

                <!-- Product Variants Section -->
                <section class="space-y-4">
                  <div class="flex justify-between items-center px-2">
                    <h3 class="text-lg font-bold text-white flex items-center gap-2 font-display">
                      <span class="material-symbols-outlined text-primary">layers</span>
                      Product Variants
                    </h3>
                  </div>

                  <!-- Variants Container -->
                  <div id="variantsContainer" class="space-y-4">

                    <!-- Variant 1 (Static First Variant) -->
                    <div
                      class="variant-box bg-[#1c2632]/30 border border-[#1c2632] rounded-xl overflow-hidden p-6 space-y-6"
                      data-index="1">
                      <div class="flex justify-between items-center">
                        <span class="text-xs font-black uppercase tracking-widest text-primary">Variant 1</span>
                        <button type="button" onclick="removeVariant(this)"
                          class="text-slate-500 hover:text-red-500 transition-colors">
                          <span class="material-symbols-outlined">delete</span>
                        </button>
                      </div>

                      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <!-- SKU -->
                        <div class="space-y-1">
                          <label class="text-xs font-bold text-slate-400 uppercase tracking-wider">SKU <span
                              class="text-red-400">*</span></label>
                          <input id="sku_1" name="sku_1"
                            class="w-full bg-[#101922] border border-[#1c2632] rounded-lg focus:ring-primary focus:border-primary text-white text-sm px-3 py-2"
                            placeholder="FB-GLV-001" type="text" />
                          <p id="skuError_1" class="error-msg hidden text-red-400 text-xs mt-1"></p>
                        </div>
                        <!-- Stock -->
                        <div class="space-y-1">
                          <label class="text-xs font-bold text-slate-400 uppercase tracking-wider">Stock Quantity <span
                              class="text-red-400">*</span></label>
                          <input id="stock_1" name="stock_1"
                            class="w-full bg-[#101922] border border-[#1c2632] rounded-lg focus:ring-primary focus:border-primary text-white text-sm px-3 py-2"
                            placeholder="100" type="number" min="0" />
                          <p id="stockError_1" class="error-msg hidden text-red-400 text-xs mt-1"></p>
                        </div>
                        <!-- Color -->
                        <div class="space-y-1">
                          <label class="text-xs font-bold text-slate-400 uppercase tracking-wider">Color <span
                              class="text-red-400">*</span></label>
                          <input id="color_1" name="color_1"
                            class="w-full bg-[#101922] border border-[#1c2632] rounded-lg focus:ring-primary focus:border-primary text-white text-sm px-3 py-2"
                            placeholder="e.g. Neon Green" type="text" />
                          <p id="colorError_1" class="error-msg hidden text-red-400 text-xs mt-1"></p>
                        </div>
                        <!-- Size -->
                        <div class="space-y-1">
                          <label class="text-xs font-bold text-slate-400 uppercase tracking-wider">Size <span
                              class="text-red-400">*</span></label>
                          <select id="size_1" name="size_1"
                            class="w-full bg-[#101922] border border-[#1c2632] rounded-lg focus:ring-primary focus:border-primary text-white text-sm px-3 py-2">
                            <option value="">Select Size</option>
                            <option value="S">S</option>
                            <option value="M">M</option>
                            <option value="L">L</option>
                            <option value="XL">XL</option>
                          </select>
                          <p id="sizeError_1" class="error-msg hidden text-red-400 text-xs mt-1"></p>
                        </div>
                        <!-- Price -->
                        <div class="space-y-1">
                          <label class="text-xs font-bold text-slate-400 uppercase tracking-wider">Price (₹) <span
                              class="text-red-400">*</span></label>
                          <input id="price_1" name="price_1"
                            class="w-full bg-[#101922] border border-[#1c2632] rounded-lg focus:ring-primary focus:border-primary text-white text-sm px-3 py-2"
                            placeholder="49.99" type="number" min="0" step="0.01" />
                          <p id="priceError_1" class="error-msg hidden text-red-400 text-xs mt-1"></p>
                        </div>
                        <!-- Discounted Price -->
                        <div class="space-y-1">
                          <label class="text-xs font-bold text-slate-400 uppercase tracking-wider">Discounted Price
                            (₹)</label>
                          <input id="discountedPrice_1" name="discountedPrice_1"
                            class="w-full bg-[#101922] border border-[#1c2632] rounded-lg focus:ring-primary focus:border-primary text-white text-sm px-3 py-2"
                            placeholder="39.99" type="number" min="0" step="0.01" />
                          <p id="discountedPriceError_1" class="error-msg hidden text-red-400 text-xs mt-1"></p>
                        </div>
                      </div>

                      <!-- Variant Images -->
                      <div class="space-y-3 pt-4 border-t border-[#1c2632]">
                        <div class="flex flex-col gap-1">
                          <label class="text-xs font-bold text-slate-400 uppercase tracking-wider">Variant Images <span
                              class="text-red-400">*</span></label>
                          <p class="text-[10px] text-slate-500 uppercase tracking-widest font-black">Max 3 images for
                            this variant</p>
                        </div>
                        <div class="relative">
                          <label for="variantImages_1"
                            class="flex flex-col items-center justify-center w-full h-28 border-2 border-dashed border-[#1c2632] rounded-lg cursor-pointer hover:bg-[#1c2632]/40 transition-colors group">
                            <span
                              class="material-symbols-outlined text-slate-500 group-hover:text-primary">add_a_photo</span>
                            <span class="text-xs font-bold text-slate-500 group-hover:text-primary mt-1">CLICK TO
                              UPLOAD</span>
                          </label>
                          <input id="variantImages_1" name="variantImages_1" type="file" accept="image/*" multiple
                            class="hidden" onchange="previewImages(this, 1)" />
                        </div>
                        <div id="imagePreview_1" class="grid grid-cols-4 gap-3 mt-2"></div>
                        <p id="imagesError_1" class="error-msg hidden text-red-400 text-xs mt-1"></p>
                      </div>
                    </div>

                    <!-- Add Another Variant Button -->
                    <button id="addVariantBtn" type="button" onclick="addVariant()"
                      class="w-full py-4 border-2 border-dashed border-[#1c2632] rounded-xl flex items-center justify-center gap-2 text-slate-400 hover:text-white hover:bg-[#1c2632]/30 transition-all font-bold text-sm">
                      <span class="material-symbols-outlined">add_circle</span>
                      Add Another Variant
                    </button>
                  </div>
                </section>
                <!-- Form Actions -->
                <div class="flex items-center justify-end gap-3 pt-12">
                  <a href="/admin/products">
                    <button type="button"
                      class="px-6 py-2.5 rounded text-slate-300 font-bold hover:text-white transition-colors text-sm">
                      Cancel
                    </button>
                  </a>
                  <button id="submitProductBtn" type="submit"
                    class="px-8 py-2.5 rounded bg-primary text-white text-sm font-black shadow-lg shadow-primary/20 hover:scale-[1.02] active:scale-95 transition-all flex items-center gap-2 disabled:opacity-60 disabled:cursor-not-allowed">
                    <span class="material-symbols-outlined text-sm font-bold">save</span>
                    SAVE PRODUCT
                  </button>
                </div>
              </div>

              <!-- Right Column -->
              <div class="space-y-8">
                <!-- (Reserved for future sidebar content) -->
              </div>

            </div>
          </form>
          <!-- ═══════════════ END FORM ═══════════════ -->

        </div>
      </div>
    </main>
  </div>

  <!-- ═══════════════ IMAGE CROP MODAL ═══════════════ -->
  <div id="cropModal"
    class="fixed inset-0 z-[9999] flex items-center justify-center bg-black/80 backdrop-blur-sm hidden">
    <div
      class="bg-[#1c2632] rounded-2xl shadow-2xl border border-[#2a3a4e] w-full max-w-2xl mx-4 flex flex-col overflow-hidden"
      style="max-height:90vh">
      <!-- Header -->
      <div class="flex items-center justify-between px-6 py-4 border-b border-[#2a3a4e] flex-shrink-0">
        <div class="flex items-center gap-2">
          <span class="material-symbols-outlined text-primary">crop</span>
          <h3 class="text-white font-bold font-display text-base">Crop Image</h3>
          <span id="cropImageCounter"
            class="hidden text-xs text-slate-400 bg-[#101922] px-2 py-0.5 rounded-full font-bold"></span>
        </div>
        <button type="button" id="cropCancelBtn" class="text-slate-400 hover:text-red-400 transition-colors">
          <span class="material-symbols-outlined">close</span>
        </button>
      </div>
      <!-- Crop Canvas Area -->
      <div class="bg-[#0d1520] flex-1 overflow-hidden" style="min-height:300px;max-height:420px;">
        <img id="cropImage" src="" style="display:block;max-width:100%;" alt="crop target" />
      </div>
      <!-- Aspect Ratio Buttons -->
      <div class="flex items-center gap-2 px-6 py-3 border-t border-[#2a3a4e] bg-[#111418] flex-shrink-0">
        <span class="text-xs text-slate-400 font-bold uppercase tracking-wider mr-1">Ratio:</span>
        <button type="button" data-ratio="1" onclick="setCropAspect(1,    this)"
          class="crop-ratio-btn px-3 py-1 text-xs rounded border border-[#2a3a4e] text-slate-300 hover:border-primary hover:text-primary transition-colors font-bold">1:1</button>
        <button type="button" data-ratio="1.33" onclick="setCropAspect(4/3,  this)"
          class="crop-ratio-btn px-3 py-1 text-xs rounded border border-[#2a3a4e] text-slate-300 hover:border-primary hover:text-primary transition-colors font-bold">4:3</button>
        <button type="button" data-ratio="1.78" onclick="setCropAspect(16/9, this)"
          class="crop-ratio-btn px-3 py-1 text-xs rounded border border-[#2a3a4e] text-slate-300 hover:border-primary hover:text-primary transition-colors font-bold">16:9</button>
        <button type="button" data-ratio="nan" onclick="setCropAspect(NaN,  this)"
          class="crop-ratio-btn px-3 py-1 text-xs rounded border border-[#2a3a4e] text-slate-300 hover:border-primary hover:text-primary transition-colors font-bold">Free</button>
      </div>
      <!-- Footer -->
      <div class="flex items-center justify-between px-6 py-4 border-t border-[#2a3a4e] flex-shrink-0">
        <p class="text-xs text-slate-500">Drag to reposition &bull; Scroll to zoom</p>
        <div class="flex gap-3">
          <button type="button" id="cropSkipBtn"
            class="px-4 py-2 text-sm text-slate-300 hover:text-white font-bold transition-colors rounded-lg hover:bg-white/5">Skip
            Crop</button>
          <button type="button" id="cropConfirmBtn"
            class="px-5 py-2 text-sm bg-primary text-white font-bold rounded-lg hover:bg-primary/90 transition-all flex items-center gap-2 disabled:opacity-60">
            <span class="material-symbols-outlined text-sm">check</span> Apply Crop
          </button>
        </div>
      </div>
    </div>
  </div>
  <!-- ═══════════════ END CROP MODAL ═══════════════ -->

  <!-- Image Cropper Utility -->
  <script src="/adminJs/imageCropper.js"></script>
  <!-- Product Validation Script -->
  <script src="/adminJs/productValidation.js"></script>
</body>

</html>